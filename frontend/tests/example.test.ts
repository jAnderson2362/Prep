import { describe, it, expect } from 'vitest'

// Template Vitest file — update or copy for your tests.
describe('Example test', () => {
  it('basic arithmetic works', () => {
    expect(1 + 1).toBe(2)
  })
})
